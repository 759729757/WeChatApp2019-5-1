var filter = {
  isAct: function (refer, value) {
    if (refer.indexOf(value) != -1)
      return 'active'
    else
      return ""
  },
  combine: function (v1, v2) {
    return v1+v2
  },
}
module.exports = {
  isAct: filter.isAct,
  combine: filter.combine
}